# 同步机制说明

## 📋 核心要求

**A设备进行新建、编辑、删除等操作都要触发同步到服务器，然后B设备刷新网页后从服务器进行更新数据**

---

## ✅ 已实现的同步机制

### 1. A设备：所有操作都触发同步到服务器

#### 新建操作
- ✅ `addFolder()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `addNote()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `addUrl()` → 调用 `syncDataToServer()`（延迟500ms，防抖）

#### 更新操作
- ✅ `updateFolder()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `updateNote()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `updateUrl()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `toggleFolderStar()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `toggleNoteStar()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `toggleUrlStar()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `changeFolderColor()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- ✅ `reorderFolder()` → 调用 `syncDataToServer()`（延迟500ms，防抖）

#### 删除操作
- ✅ `deleteFolder()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）
- ✅ `deleteNote()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）
- ✅ `deleteUrl()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）

#### 恢复操作
- ✅ `restoreFromTrash()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）
- ✅ `permanentlyDelete()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）

### 2. B设备：刷新网页后自动从服务器同步

#### 自动同步触发时机
- ✅ **登录时**：登录成功后1秒自动从服务器同步数据
- ✅ **页面加载时**：页面加载完成后1秒自动从服务器同步数据（刷新网页时）
- ✅ **页面可见性变化时**：页面从隐藏变为可见时，延迟500ms自动从服务器同步数据
- ✅ **页面获得焦点时**：切换回标签页时，延迟500ms自动从服务器同步数据

#### 同步逻辑
- ✅ 所有同步都使用 `syncDataFromServer(0, true)`，强制优先使用服务器数据
- ✅ 确保服务器数据始终是权威来源

---

## 🔄 同步流程

### A设备操作流程

1. **用户操作**（新建/编辑/删除）
   - 更新本地状态
   - 标记 `pendingChanges = true`

2. **触发同步**
   - 删除操作：立即同步（`immediateSync`）
   - 其他操作：延迟500ms同步（`debouncedUploadSync`，防抖优化）

3. **同步到服务器**
   - 调用 `syncDataToServer()`
   - 发送数据到服务器：`POST /api/v1/data/sync`
   - 服务器保存数据

4. **上传成功后**
   - 立即从服务器拉取最新数据
   - 强制应用服务器数据（确保一致性）

### B设备刷新流程

1. **页面加载**
   - 页面加载完成后，延迟1秒自动从服务器同步数据

2. **页面可见性变化**
   - 页面从隐藏变为可见时，延迟500ms自动从服务器同步数据

3. **页面获得焦点**
   - 切换回标签页时，延迟500ms自动从服务器同步数据

4. **从服务器同步**
   - 调用 `syncDataFromServer(0, true)`
   - 从服务器获取数据：`GET /api/v1/data/sync`
   - 强制优先使用服务器数据（`prioritizeServer=true`）

---

## 📊 同步状态管理

### 状态字段
- `pendingChanges`: 是否有待同步的变更
- `isUploading`: 是否正在上传
- `isDownloading`: 是否正在下载
- `lastSyncTime`: 最后同步时间
- `syncError`: 同步错误信息
- `syncSuccess`: 最近一次同步是否成功

### 同步队列
- 使用 `syncQueue` 确保操作串行执行
- 避免并发冲突

---

## ⚠️ 注意事项

1. **删除操作优先级**
   - 删除操作使用 `immediateSync` 立即同步
   - 确保删除状态能及时同步到服务器

2. **服务器数据优先**
   - 所有合并操作默认 `prioritizeServer=true`
   - 确保服务器数据始终是权威来源

3. **刷新网页时自动同步**
   - 页面加载时自动同步
   - 页面可见性变化时自动同步
   - 页面获得焦点时自动同步

4. **多设备一致性**
   - 所有设备都从服务器同步数据
   - 服务器数据是唯一真实来源

---

## ✅ 总结

当前的同步机制**完全符合要求**：

1. ✅ **A设备所有操作都触发同步到服务器**
   - 所有新建/更新/删除操作都会调用 `syncDataToServer()`
   - 删除操作立即同步，其他操作延迟同步（防抖优化）

2. ✅ **B设备刷新网页后自动从服务器同步**
   - 页面加载时自动同步
   - 页面可见性变化时自动同步
   - 页面获得焦点时自动同步

**核心保证：A设备的所有变更都会同步到服务器，B设备刷新网页后会自动从服务器获取最新数据。**



























