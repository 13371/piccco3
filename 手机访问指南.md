# 📱 手机访问指南

## ✅ 您的电脑信息

- **电脑IP地址**: `192.168.0.67`
- **前端地址**: `http://192.168.0.67:5173`
- **后端地址**: `http://192.168.0.67:4000`

---

## 📲 在手机上访问的步骤

### 1. 确保手机和电脑在同一网络

- ✅ 手机和电脑必须连接到**同一个WiFi网络**
- ❌ 如果手机用流量，电脑用WiFi，则无法访问

### 2. 在手机浏览器中打开

打开手机浏览器（Chrome、Safari、Edge等），输入：

```
http://192.168.0.67:5173
```

**注意**：使用 `http://` 而不是 `https://`

### 3. 如果无法访问，检查防火墙

#### Windows 防火墙设置

1. **打开 Windows 防火墙**
   - 按 `Win + R`，输入 `wf.msc`，回车

2. **添加入站规则**
   - 点击左侧"入站规则"
   - 点击右侧"新建规则"
   - 选择"端口" → 下一步
   - 选择"TCP"，输入端口：`5173,4000` → 下一步
   - 选择"允许连接" → 下一步
   - 全部勾选（域、专用、公用）→ 下一步
   - 名称：`piccco开发服务器` → 完成

#### 或者使用 PowerShell 快速添加（推荐）

```powershell
# 允许前端端口 5173
New-NetFirewallRule -DisplayName "piccco-frontend" -Direction Inbound -LocalPort 5173 -Protocol TCP -Action Allow

# 允许后端端口 4000
New-NetFirewallRule -DisplayName "piccco-backend" -Direction Inbound -LocalPort 4000 -Protocol TCP -Action Allow
```

---

## 🔍 测试连接

### 在手机上测试后端是否可访问

在手机浏览器中打开：

```
http://192.168.0.67:4000
```

如果看到 JSON 响应（显示 API 信息），说明后端可以访问。

### 在手机上测试前端是否可访问

在手机浏览器中打开：

```
http://192.168.0.67:5173
```

如果看到登录/注册页面，说明前端可以访问。

---

## ⚠️ 常见问题

### 问题1：手机显示"无法访问此网站"

**解决方案**：
1. 确认手机和电脑在同一WiFi
2. 检查Windows防火墙是否阻止了端口
3. 确认服务器正在运行（端口 5173 和 4000）

### 问题2：页面加载但无法登录/注册

**解决方案**：
1. 打开手机浏览器的开发者工具（如果支持）
2. 查看控制台错误信息
3. 确认后端API地址配置正确

### 问题3：验证码看不到

**解决方案**：
1. 在手机浏览器中，打开开发者工具（如果支持）
2. 或者查看电脑上的后端控制台输出
3. 验证码会显示在控制台

---

## 🎯 快速访问地址

**前端（手机访问）**: `http://192.168.0.67:5173`

**后端API**: `http://192.168.0.67:4000`

---

## 📝 注意事项

1. ✅ 确保前端和后端服务器都在运行
2. ✅ 确保手机和电脑在同一WiFi网络
3. ✅ 如果更换网络，IP地址可能会改变，需要重新查询
4. ✅ 开发模式下，验证码会显示在电脑的后端控制台

---

## 🔄 如果IP地址改变了

如果电脑的IP地址改变了，重新查询IP地址：

**Windows PowerShell**:
```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.IPAddress -notlike "127.*" -and $_.IPAddress -notlike "169.254.*"} | Select-Object IPAddress
```

**Windows CMD**:
```cmd
ipconfig | findstr IPv4
```

然后使用新的IP地址访问。

---

现在您可以在手机上访问了！🎉




























