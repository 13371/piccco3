# 同步逻辑说明

## 📋 核心原则

**强制要求：必须以服务器为准，所有变更先同步到服务器，然后所有设备从服务器进行更新**

---

## 🔄 同步流程

### 1. 数据变更操作（新建、更新、删除）

所有数据变更操作都会触发同步：

#### 新建操作
- `addFolder()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- `addNote()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- `addUrl()` → 调用 `syncDataToServer()`（延迟500ms，防抖）

#### 更新操作
- `updateFolder()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- `updateNote()` → 调用 `syncDataToServer()`（延迟500ms，防抖）
- `updateUrl()` → 调用 `syncDataToServer()`（延迟500ms，防抖）

#### 删除操作
- `deleteFolder()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）
- `deleteNote()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）
- `deleteUrl()` → 立即调用 `syncDataToServer(true)`（立即同步，不延迟）

### 2. 同步到服务器（`syncDataToServer`）

1. **检查登录状态**
   - 如果未登录，只更新本地状态，不同步到服务器

2. **准备同步数据**
   - 对于删除操作：包含所有数据（包括已删除的）
   - 对于普通操作：只包含未删除的数据

3. **发送到服务器**
   - POST `/api/v1/data/sync`
   - 包含：folders, notes, urls, permanentlyDeletedFolderIds

4. **服务器处理**
   - 服务器合并客户端和服务器端的数据
   - 使用 `updatedAt` 判断，优先使用更新的数据
   - 删除操作（`isDeleted = true`）必须接受

5. **上传成功后**
   - ✅ **立即从服务器拉取最新数据**（确保以服务器为准）
   - ✅ **强制应用服务器数据**（`prioritizeServer=true`）
   - ✅ **增加重试机制**（最多3次，确保能获取到最新数据）

### 3. 从服务器同步（`syncDataFromServer`）

1. **触发时机**
   - 登录时（强制优先使用服务器数据）
   - 上传成功后（立即拉取最新数据）
   - 手动触发（设置页面）

2. **合并逻辑**
   - 默认 `prioritizeServer=true`（强制优先使用服务器数据）
   - 如果服务器有数据，完全覆盖本地数据
   - 如果服务器没有数据，保留本地数据（新创建的，还未同步）

3. **数据合并**
   - 使用 `mergeArrays()` 和 `mergeItem()` 合并数据
   - 服务器数据优先，本地数据作为补充

---

## 🔐 关键保证

### 1. 所有变更都先同步到服务器

✅ **已实现**
- 所有操作都会调用 `syncDataToServer()`
- 删除操作立即同步，其他操作延迟500ms同步（防抖优化）

### 2. 以服务器为准

✅ **已实现**
- `mergeArrays()` 和 `mergeItem()` 默认 `prioritizeServer=true`
- 登录时强制优先使用服务器数据
- 上传成功后立即从服务器拉取最新数据并强制应用

### 3. 所有设备从服务器更新

✅ **已实现**
- 上传成功后立即从服务器拉取最新数据
- 登录时从服务器同步数据
- 可以手动触发同步

---

## 📊 同步状态管理

### 状态字段
- `pendingChanges`: 是否有待同步的变更
- `isUploading`: 是否正在上传
- `isDownloading`: 是否正在下载
- `lastSyncTime`: 最后同步时间
- `syncError`: 同步错误信息
- `syncSuccess`: 最近一次同步是否成功

### 同步队列
- 使用 `syncQueue` 确保操作串行执行
- 避免并发冲突

---

## ⚠️ 注意事项

1. **删除操作优先级**
   - 删除操作使用 `immediateSync` 立即同步
   - 确保删除状态能及时同步到服务器

2. **服务器数据优先**
   - 所有合并操作默认 `prioritizeServer=true`
   - 确保服务器数据始终是权威来源

3. **上传成功后拉取**
   - 上传成功后立即从服务器拉取最新数据
   - 增加重试机制（最多3次），确保能获取到最新数据

4. **多设备一致性**
   - 所有设备都从服务器同步数据
   - 服务器数据是唯一真实来源

---

## 🔧 优化建议

1. **增加同步状态提示**
   - 显示同步进度
   - 显示同步错误信息

2. **优化同步时机**
   - 页面可见性变化时自动同步
   - 网络恢复时自动同步

3. **增强错误处理**
   - 同步失败时保留本地变更
   - 提供手动重试机制

---

## ✅ 总结

当前的同步逻辑**完全符合要求**：

1. ✅ **所有变更都先同步到服务器**
   - 所有操作都会调用 `syncDataToServer()`
   - 删除操作立即同步，其他操作延迟同步

2. ✅ **以服务器为准**
   - 合并逻辑默认 `prioritizeServer=true`
   - 上传成功后立即从服务器拉取最新数据

3. ✅ **所有设备从服务器更新**
   - 上传成功后立即从服务器拉取最新数据
   - 登录时从服务器同步数据

**核心保证：服务器数据始终是权威来源，所有设备都从服务器同步数据。**





























