# 🚀 前后端服务器启动信息

## ✅ 服务器状态

### 后端服务器
- **状态**: ✅ 已启动
- **端口**: 4000
- **API地址**: http://localhost:4000/api
- **健康检查**: http://localhost:4000/api/health

### 前端服务器
- **状态**: ✅ 已启动
- **端口**: 5173 (Vite默认端口)
- **访问地址**: http://localhost:5173

---

## 📋 启动方式

### 方式一：使用已启动的窗口
两个PowerShell窗口已最小化打开，您可以：
1. 在任务栏找到最小化的PowerShell窗口
2. 点击窗口查看服务器日志
3. 确认服务器正常运行

### 方式二：手动启动（如果需要）

#### 启动后端服务器
```powershell
cd backend
npm start
```

#### 启动前端服务器
```powershell
npm run dev
```

---

## 🔍 验证服务器运行

### 检查后端服务器
打开浏览器访问：
- http://localhost:4000/api/health

应该看到健康检查响应。

### 检查前端服务器
打开浏览器访问：
- http://localhost:5173

应该看到应用界面。

---

## 🛠️ 常用命令

### 查看端口占用
```powershell
netstat -ano | findstr ":4000"
netstat -ano | findstr ":5173"
```

### 停止服务器
在对应的PowerShell窗口中按 `Ctrl+C`

### 重启服务器
1. 停止当前服务器（Ctrl+C）
2. 重新运行启动命令

---

## 📝 注意事项

1. **端口占用**: 如果端口已被占用，请先关闭占用端口的进程
2. **环境变量**: 确保后端 `.env` 文件配置正确
3. **依赖安装**: 如果首次运行，请先执行 `npm install`

---

## 🎯 下一步

1. ✅ 确认后端服务器运行在 http://localhost:4000
2. ✅ 确认前端服务器运行在 http://localhost:5173
3. ✅ 在浏览器中访问前端应用
4. ✅ 测试登录/注册功能
5. ✅ 测试数据同步功能

---

**启动时间**: $(Get-Date)  
**状态**: 运行中 ✅








