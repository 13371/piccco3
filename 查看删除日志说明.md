# 如何查看删除操作的日志

## 方法 1：在 Cursor 终端中查看（推荐）

1. **停止当前后台运行的服务器**
   ```powershell
   Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
   ```

2. **在 Cursor 终端中以前台模式启动服务器**
   ```powershell
   cd backend
   node src/server.js
   ```

3. **执行删除操作**，日志会直接显示在终端中：
   ```
   删除后 folders = [
     {
       "id": "...",
       "name": "...",
       "isDeleted": true,
       "deletedAt": 1234567890,
       "updatedAt": 1234567890,
       ...
     },
     ...
   ]
   ```

## 方法 2：将日志输出到文件

1. **停止当前服务器**
   ```powershell
   Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force
   ```

2. **启动服务器并将输出重定向到文件**
   ```powershell
   cd backend
   node src/server.js > server.log 2>&1
   ```

3. **查看日志文件**
   ```powershell
   Get-Content server.log -Tail 50 -Wait
   ```
   或者直接打开 `backend/server.log` 文件查看

## 方法 3：使用 PowerShell 查看实时日志

```powershell
# 启动服务器并实时查看输出
cd backend
node src/server.js | Tee-Object -FilePath server.log
```

这样既可以在控制台看到实时输出，也会保存到文件中。

## 注意事项

- 删除操作会输出完整的 `folders` 数组（JSON 格式）
- 日志包含所有文件夹，包括被删除的（`isDeleted: true`）和未删除的
- 如果使用后台模式，需要查看日志文件或使用上述方法切换到前台模式






