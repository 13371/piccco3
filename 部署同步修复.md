# 同步问题修复部署说明

## 修复内容

本次修复解决了以下4个同步问题：

1. ✅ **首页大白框内容不同步** - 修复了快照更新逻辑，确保使用实际更新后的homeContent
2. ✅ **新建URL不同步** - 已确认同步逻辑正确，新建URL会被正确检测并同步
3. ✅ **隐私文件夹密码不同步** - 添加了password字段变化检测，确保密码变化时同步
4. ✅ **新建记事文本时间不对** - 修复了createdAt保留逻辑，确保正确保留创建时间

## 代码已提交

- ✅ 代码已提交到 GitHub: `54f62a1`
- ✅ 提交信息: "修复同步问题：1.首页内容不同步 2.隐私文件夹密码不同步 3.新建记事时间不对"

## 部署步骤

### 方法1：使用部署脚本（推荐）

#### 前端部署

```bash
# 1. SSH 连接到服务器
ssh your-user@your-server-ip

# 2. 进入项目目录
cd /www/wwwroot/piccco3

# 3. 拉取最新代码
git pull origin main

# 4. 执行前端部署脚本
chmod +x deploy-frontend.sh
./deploy-frontend.sh
```

#### 后端部署（如果需要）

```bash
# 1. 进入后端目录
cd /www/wwwroot/piccco3/backend

# 2. 执行后端部署脚本
chmod +x deploy.sh
./deploy.sh
```

### 方法2：手动部署

#### 前端手动部署

```bash
# 1. SSH 连接到服务器
ssh your-user@your-server-ip

# 2. 进入项目目录
cd /www/wwwroot/piccco3

# 3. 拉取最新代码
git pull origin main

# 4. 安装依赖（如果需要）
npm install

# 5. 构建前端
npm run build

# 6. 检查构建结果
ls -la dist/
```

#### 配置 Nginx（如果需要）

如果使用 Nginx，确保配置指向 `dist/` 目录：

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /www/wwwroot/piccco3/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理到后端
    location /api {
        proxy_pass http://localhost:4000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

然后重启 Nginx：

```bash
nginx -t  # 测试配置
systemctl restart nginx  # 重启 Nginx
```

### 方法3：使用宝塔面板

如果使用宝塔面板：

1. **文件管理** → 进入 `/www/wwwroot/piccco3`
2. **终端** → 执行以下命令：
   ```bash
   git pull origin main
   npm install
   npm run build
   ```
3. **网站** → 选择网站 → **设置** → **网站目录** → 指向 `/www/wwwroot/piccco3/dist`
4. **保存** 并 **重启 Nginx**

## 验证部署

### 1. 检查代码版本

```bash
cd /www/wwwroot/piccco3
git log -1
# 应该看到提交: 54f62a1
```

### 2. 检查构建文件

```bash
ls -la dist/
# 应该看到 index.html 和 assets/ 目录
```

### 3. 清除浏览器缓存

部署后，**必须清除浏览器缓存**才能看到更新：

- **Chrome/Edge**: `Ctrl+Shift+Delete` 或 `Cmd+Shift+Delete`
- **Firefox**: `Ctrl+Shift+Delete` 或 `Cmd+Shift+Delete`
- 或者使用**硬刷新**: `Ctrl+F5` 或 `Cmd+Shift+R`

### 4. 测试同步功能

部署后，请在多个设备上测试：

1. **首页内容同步** - 在A设备编辑首页内容，检查B设备是否同步
2. **URL同步** - 在A设备新建URL，检查B设备是否显示
3. **隐私密码同步** - 在A设备设置隐私密码，检查B设备是否可用相同密码
4. **记事时间** - 新建记事，检查创建时间是否正确

## 注意事项

1. ⚠️ **必须清除浏览器缓存**才能看到更新
2. ⚠️ 确保 Nginx 配置正确指向 `dist/` 目录
3. ⚠️ 如果使用 PM2 管理后端，确保后端服务正常运行
4. ⚠️ 部署后建议在多个设备上测试同步功能

## 回滚（如果需要）

如果部署后出现问题，可以回滚到上一个版本：

```bash
cd /www/wwwroot/piccco3
git log  # 查看提交历史
git reset --hard <previous-commit-hash>  # 回滚到指定提交
npm run build  # 重新构建
```

## 问题排查

如果部署后同步仍有问题：

1. 检查浏览器控制台是否有错误
2. 检查网络请求是否正常
3. 检查后端服务是否正常运行
4. 检查服务器日志：`pm2 logs piccco-backend`

---

**部署完成后，请清除浏览器缓存并测试同步功能！**

